# 项目演进说明

## 第一阶段：商品模块

目标：建立商品基础管理能力。

完成内容：

- 创建商品
- 查询商品列表
- 查询商品详情
- 商品上架
- 商品下架

设计重点：

- 商品创建后默认下架
- 价格使用 price_fen，避免浮点数问题
- 商品状态通过 status 控制

## 第二阶段：库存模块

目标：建立商品库存管理能力。

完成内容：

- 初始化库存
- 查询商品库存
- 增加库存
- 写库存流水

设计重点：

- 商品和库存分表
- 一个商品只能初始化一条库存记录
- 每次库存变化都写入 stock_logs
- 通过 before/change/after 记录库存变化链路

## 第三阶段：订单模块

目标：打通订单创建和库存扣减闭环。

完成内容：

- 创建订单
- 创建订单项
- 扣减库存
- 写库存流水
- 查询订单列表
- 查询订单详情

设计重点：

- 使用事务保证订单和库存一致
- 下单时保存商品名称和价格快照
- 使用 stock_logs 记录订单扣减库存

## 第四阶段：订单状态机

目标：补齐订单状态流转。

完成内容：

- 支付订单
- 完成订单
- 取消订单
- 取消订单回滚库存

设计重点：

- 只允许合法状态流转
- 使用 PatchOrderStatus 做条件更新
- 取消订单时回滚库存并写库存流水

## 第五阶段：Redis 缓存商品详情

目标：引入 Redis 缓存，提高商品详情查询性能。

完成内容：
- 已接入 Redis 客户端
- 商品详情接口已采用 cache-aside
- Redis miss 时查询 MySQL 并写入缓存
- 商品上架 / 下架时删除商品详情缓存
- Redis 不可用时不影响主流程

## 第六阶段：用户认证与订单隔离

目标：让订单归属于真实登录用户，并隔离不同用户的数据。

完成内容：

- 用户注册、登录和 JWT 签发
- 查询当前用户、修改昵称和修改密码
- 受保护接口统一校验 Bearer Token
- 订单创建、查询和状态更新按 user_id 隔离
- 幂等键唯一范围从全局调整为用户级

设计重点：

- 密码仅保存 bcrypt 哈希
- JWT 携带 user_id、username、issuer 和过期时间
- 跨用户访问订单按不存在处理，避免泄露资源信息
- `(user_id, idempotency_key)` 复合唯一索引支持不同用户复用相同 Key

## 第七阶段：React 管理台联调

目标：提供可演示、可操作的业务管理界面，覆盖后端主要接口。

完成内容：

- 注册、登录和前端 Token 状态管理
- 商品、库存、库存流水和订单业务页面
- 当前用户资料查询、昵称修改和密码修改
- 侧边栏与顶部菜单动态展示用户昵称和用户名
- 仪表盘通过 `/ping` 展示后端连通状态

设计重点：

- Axios 统一配置 `/api/v1` 基础路径和 Bearer Token
- TanStack Query 管理查询缓存和业务数据刷新
- 后端未支持的 OAuth、邮箱、头像等入口不作为可用功能展示
- 保留统一业务响应结构 `{ code, message, data }`，不在前端改写公共协议

## 第八阶段：项目合并

目标：结合用户基础认证系统，实现用户登录订单管理系统

完成内容：

- 合并用户注册、登录、资料维护和密码修改能力
- 订单表增加 `user_id`，查询和状态变更按用户隔离
- 幂等键调整为 `(user_id, idempotency_key)` 复合唯一约束
- 前端认证状态与后端 Bearer Token 接口完成联调

设计重点：

- 复用统一用户模型和 JWT 身份，不保留两套认证入口
- 跨用户访问订单按资源不存在处理，避免泄露订单是否存在
- 合并后执行全量迁移、接口和订单库存回归

## 第九阶段：项目完善

目标：加入最小 RBAC 权限系统，补齐关键测试，并完成待支付订单自动取消闭环。

完成内容：

- 实现最小管理员 RBAC
- 商品创建、上架/下架、库存初始化、增加库存和库存流水查询已限制为管理员可用
- 完成订单 Handler 业务接口测试和关键 DAO 集成测试
- RabbitMQ TTL/DLX + 事务 Outbox，实现“30 分钟未支付自动取消”
- 提供单一 Dockerfile（本地 Compose 与云部署共用）、Docker Compose、Makefile 和 CI workflow，支持本地/CI 环境部署与回归验证

设计重点：

- 创建商品、上架/下架商品、初始化/增加库存非管理员不可操作
- 订单创建、扣库存、库存流水和超时 Outbox 在同一事务内提交
- 超时消息至少一次投递，消费者通过订单状态条件更新保证重复消息不会重复回滚库存
- 线上云服务器部署和长期稳定性证据不在本地可复现交付范围内，作为后续增强记录

## 第十阶段：后续进阶边界

目标：在当前订单库存主链路稳定后，继续补齐可观测性、审计和性能证据，使项目水准达到更完整的中级后端项目。

当前定位：

- 以下内容属于后续增强，不作为当前版本必须完成的主链路交付标准
- 当前版本已经完成商品、库存、订单、认证、最小 RBAC、Redis 商品详情缓存、RabbitMQ 超时取消、React 管理台、后台操作审计和自动化测试闭环
- 不将未经验证的云部署、长期稳定性、支付退款链路写成已完成能力；本地压测数字只作为同机基线
- 阶段整体状态：**部分完成**。本地可复现的工程增强大部分已经落地，但仍有外部运行证据、完整追踪、写链路压测和复杂履约链路未完成

状态定义：

- **已完成**：当前约定范围已有代码、测试和文档证据。
- **部分完成**：基础能力已落地，但该项仍有明确验收内容未完成。
- **未完成**：尚未形成可验证交付物，不得写入已实现能力。
- **持续事项**：每次接口、迁移或业务规则变化后都需要继续维护。

状态总览：

| 能力 | 状态 | 已有证据 | 未完成内容 |
|---|---|---|---|
| Redis 进阶库存保护 | 已完成 | Lua 原子预扣、reservation 回补/确认、固定 hash tag、MySQL 对账与管理员重建 | 当前范围内无；Redis 仍不是库存事实源 |
| 后台操作审计和管理员边界 | 已完成 | `operation_logs`、审计中间件、管理员查询接口和 React 管理页 | 当前只覆盖约定的后台写操作，不等同于通用审计平台 |
| 多商品锁顺序治理 | 已完成 | 商品和库存按 `product_id` 升序加锁，服务测试覆盖倒序请求 | 当前范围内无；新增多资源事务时仍需保持全局锁顺序 |
| 基础可观测性 | 部分完成 | `/metrics`、订单/Redis 指标、W3C `traceparent`、Access Log、慢 SQL 和对账日志 | OpenTelemetry SDK/Collector、跨组件 span、Jaeger/Tempo 等追踪后端和线上告警证据 |
| 性能与稳定性证据 | 部分完成 | 内置压测命令、本地 Docker `/ping` 和商品列表报告、商品查询 EXPLAIN | 订单写链路、热点库存竞争、资源峰值、长时间运行和生产网络压测 |
| 线上部署与运行证据 | 未完成 | Dockerfile、Compose、健康检查和 CI 仅证明具备部署前置能力 | 云主机实际部署、域名/TLS、监控采集、故障恢复和持续运行记录 |
| 完整支付与履约链路 | 未完成 | 当前只有本地订单“支付/完成”状态流转接口 | 支付网关、支付回调幂等、退款、发货、收货、售后及对应状态机、迁移、API 和测试 |
| Markdown 与证据同步 | 持续事项 | 接口、权限、表结构、业务规则、测试结果和 `agent_log` 已同步本轮增强 | 后续每次代码、迁移、配置和运行证据变化后继续更新 |

后续跟进清单：

| 优先级 | 状态 | 任务 | 完成判定 | 前置条件 |
|---|---|---|---|---|
| P0 | 未完成 | 订单写链路与热点库存压测 | 使用动态幂等键并发创建订单；核对成功/失败分布、MySQL 库存、Redis reservation、库存流水和错误日志；提交原始报告与资源峰值 | 独立测试数据、可生成唯一请求体的场景化压测工具 |
| P1 | 未完成 | OpenTelemetry 完整追踪 | HTTP 请求可在追踪后端查看跨 Handler/Service/DB/Redis/RabbitMQ 的关联 span，并保留现有 request_id/trace_id 日志关联 | 确定 Collector 和 Jaeger/Tempo 方案；评估新增依赖 |
| P1 | 未完成 | 线上部署和稳定性证据 | 完整栈在云主机持续运行至少 24 小时；保存健康检查、监控、重启恢复、日志和部署步骤证据 | 云主机、域名/TLS 与部署权限 |
| P2 | 未完成 | 支付、退款、发货和售后链路 | 先完成业务状态机、表结构和 API 评审，再实现迁移、事务、幂等、权限、审计、测试和前端流程 | 明确使用模拟支付还是真实沙箱；确认退款和售后规则 |

跟进规则：

- 未满足“完成判定”前，状态不得改为“已完成”。
- 涉及数据库和公开 API 的 P2 任务必须先更新 `table_design.md`、`transaction_design.md`、`api_design.md` 和 `business_rules.md`，再进入编码。
- 每完成一项，都要同步 `test_plan.md`、`test_result.md` 和 `docs/agent_log/`，并附可复现命令或运行证据。

## 第十一阶段：简历能力描述完成度复核

目标：对照准备写入简历的五条项目描述，检查当前代码、配置、测试和文档证据是否足以支撑，避免把“已完成主链路”和“后续增强项”混在一起。

复核时间：2026-07-15

复核结论：

- 当前项目已经具备订单库存主链路、Redis Lua 库存预扣、用户级下单幂等、RabbitMQ 超时取消、商品详情 Cache-Aside、Docker Compose、Makefile 和 GitHub Actions 的基础工程化闭环。
- 但原始描述中存在若干过度表述：例如“彻底解决”“分布式事务”“消息必达”“自动熔断”“防止缓存击穿拖垮数据库”“全套微服务环境”。这些词需要有更强的实现、压测、监控或线上运行证据支撑。
- 简历中建议改成“当前项目已实现并可复现的能力”，不要把尚未完成的增强项写成已完成。

### 11.1 五条描述逐项复核

| 原描述能力 | 当前状态 | 已有实现证据 | 未完成 / 不建议直接写的点 | 建议简历写法 |
|---|---|---|---|---|
| 高并发防超卖：Redis Lua 预扣 + 数据库乐观锁兜底，彻底解决并发超卖 | **部分完成** | `internal/bizcache/inventory_stock_guard.go` 已实现 Lua 原子预扣、reservation、回补和确认；`internal/dao/inventory_dao.go` 使用 `stock_quantity >= ?` 条件扣减；`internal/service/order_service_test.go` 有并发防超卖测试 | 当前 DB 层更准确说是“行锁 + 条件扣减”，不是带 version 字段的严格乐观锁；创建订单仍会查商品、锁库存行并条件更新，不是完全摒弃查库；缺少订单写链路 / 热点库存压测报告，不能写“彻底解决” | “使用 Redis Lua 对热点库存做原子预扣，结合 MySQL 行锁与 `stock_quantity >= quantity` 条件扣减兜底，保证数据库库存不被扣成负数。” |
| 分布式事务一致性：RabbitMQ 延迟队列 + 本地消息表，订单创建与库存扣减最终一致，Outbox 保证消息必达，支付超时自动取消 | **部分完成** | `order_timeout_outbox` 本地消息表、RabbitMQ TTL/DLX、Publisher Confirm、手动 ACK 和超时取消消费者已实现；订单创建、库存扣减、库存流水和 Outbox 在同一 MySQL 事务内提交 | 当前系统是单体服务 + 同库事务，订单创建与库存扣减不是跨服务分布式事务；Outbox 当前用于“待支付订单超时取消事件”，不是用于订单与库存扣减最终一致；缺少消息积压告警、死信处理演示、长期运行证据，因此不要写“消息必达”这种绝对表述；没有真实支付网关 / 支付回调 | “使用本地 Outbox + RabbitMQ TTL/DLX 实现待支付订单超时取消；订单创建事务内同时写入订单、库存流水和超时事件，发布端使用 Confirm，消费端通过订单状态条件更新保证重复消息幂等。” |
| 接口幂等性：基于 `user_id + 业务唯一键` 的防重令牌机制，配合唯一索引拦截重复下单 | **基本完成** | `order_idempotency_keys` 表已具备 `(user_id, idempotency_key)` 复合唯一索引；`request_hash` 校验相同 Key 是否复用到不同请求；并发相同 Key 测试已覆盖 | 当前只覆盖“创建订单接口”，不是所有写接口；`idempotency_key` 是客户端提交的业务幂等键，不是服务端预生成的一次性防重令牌；幂等记录过期清理策略尚未实现 | “订单创建接口基于 `(user_id, idempotency_key)` 唯一索引和请求摘要实现用户级幂等，支持重复请求返回原订单、不同请求返回冲突。” |
| 缓存架构优化：Cache-Aside，商品上下架主动失效；Redis 故障自动熔断并降级 MySQL，防止缓存击穿拖垮数据库 | **部分完成** | `ProductCache` 实现商品详情 Cache-Aside；商品上架 / 下架删除缓存；Redis nil、读取失败时降级查 MySQL；缓存 TTL 已实现 | 未实现自动熔断器；未实现缓存击穿保护（如 singleflight、互斥重建、热点 Key 保护）；未实现缓存穿透空值缓存；商品缓存没有命中率 / miss 率指标；因此不能写“自动熔断”和“防止缓存击穿拖垮数据库” | “商品详情采用 Cache-Aside，Redis 异常时降级查询 MySQL；商品上下架主动删除详情缓存。缓存击穿保护和熔断器作为后续增强。” |
| 工程化落地：Docker Compose 编排 App/MySQL/Redis/MQ，Makefile 封装命令，GitHub Actions 自动 Lint 与镜像构建 | **基本完成** | `compose.yml` 编排 App、MySQL、Redis、RabbitMQ 和迁移任务；`Makefile` 覆盖运行、测试、迁移、Docker、压测；`.github/workflows/ci.yml` 执行 golangci-lint、test、race、vet、build 和 Docker build | 这是“单体应用 + 基础设施依赖”的本地/CI 编排，不是多个业务微服务；CI 只构建镜像，未推送镜像仓库，未做生产部署；缺少云主机长期运行证据 | “使用 Docker Compose 编排应用及 MySQL/Redis/RabbitMQ 依赖，Makefile 统一封装开发测试命令，GitHub Actions 完成 lint、测试、race、构建和 Docker 镜像构建。” |

### 11.2 当前最需要补齐的未完成项

| 优先级 | 未完成项 | 原因 | 修改建议 | 验收示例 |
|---|---|---|---|---|
| P0 | 订单写链路与热点库存压测证据 | 目前已有读链路压测和并发单元 / 集成测试，但没有覆盖真实 HTTP 下单、动态幂等 Key、Redis 预扣削峰和 MySQL 最终库存核对 | 扩展 `cmd/loadtest` 或新增脚本，支持登录、创建订单、动态生成 `idempotency_key`，压测后核对订单成功数、库存、库存流水、Redis reservation 和错误分布 | `docs/evidence/` 新增订单写链路压测报告：请求数、并发、成功/库存不足分布、最终 MySQL 库存不为负、库存流水数量正确、Redis 对账无异常 |
| P0 | 缓存击穿保护 / 熔断器 | 当前只有 Redis 单次调用超时和失败降级，没有熔断状态，也没有热点 Key 重建互斥 | 若要在简历中写“防击穿 / 熔断”，先实现 singleflight 或互斥重建；Redis 连续失败后进入短时间熔断窗口；补充指标与测试 | Redis 故障模拟下，短时间内不反复打 Redis；热点商品缓存失效时只有一个请求回源 MySQL，其余等待或复用结果 |
| P1 | Outbox 可靠性运行证据和告警 | 代码已有 Outbox、Confirm 和重试，但“消息必达”需要积压监控、失败告警、死信处理和长期运行证据 | 增加 Outbox pending/failed 指标、日志告警建议、失败事件查询接口或运维 SQL；补充 RabbitMQ 异常恢复测试 | 停 RabbitMQ 后创建订单，Outbox 保留待发布；恢复 RabbitMQ 后自动发布并取消超时订单；文档记录命令和结果 |
| P1 | 幂等记录生命周期管理 | 幂等表会随下单增长，长期运行需要清理策略 | 增加按 `created_at` 清理历史幂等记录的后台任务或管理员命令；清理前确认订单关联和保留周期 | 文档说明保留策略；测试覆盖已完成老记录可清理、进行中记录不清理 |
| P1 | CI 镜像发布与部署证据 | 当前 CI 只 build Docker image，不 push，不部署 | 如需要展示工程化闭环，可增加 GHCR/Docker Hub 推送和部署文档；或明确当前只做到 CI 构建 | GitHub Actions 产出镜像 tag，README 记录拉取运行命令 |
| P2 | 真实支付回调 / 退款 / 履约链路 | 当前只有本地“支付订单”状态接口，没有支付网关回调、退款、发货等复杂业务 | 若要写“支付超时”“复杂状态流转”，先明确模拟支付还是真实沙箱，再设计回调幂等、退款和履约状态机 | 新增表结构、API、状态机、回调幂等测试和文档 |

### 11.3 当前安全可用的简历版本

可写：

```text
高并发库存扣减：使用 Redis Lua 对热点库存做原子预扣，并通过 reservation 支持事务失败、取消订单和超时取消后的库存回补；MySQL 侧使用事务、库存行锁和 stock_quantity >= quantity 条件扣减作为最终一致性兜底，保证数据库库存不被扣成负数。

订单超时一致性：基于本地 Outbox + RabbitMQ TTL/DLX 实现待支付订单自动取消。订单创建事务内同时写入订单、库存流水和超时事件；发布端使用 Publisher Confirm，消费端通过订单状态条件更新保证重复消息幂等。

下单幂等：订单创建接口基于 user_id + idempotency_key 复合唯一索引和 SHA-256 请求摘要实现用户级幂等，拦截重复提交和相同 Key 的不同请求复用。

缓存设计：商品详情采用 Cache-Aside 模式，Redis miss 后回源 MySQL 并写缓存；商品上下架主动删除缓存，Redis 异常时降级查询 MySQL，保证缓存故障不影响主流程。

工程化：使用 Docker Compose 编排应用、MySQL、Redis、RabbitMQ 和迁移任务；Makefile 封装运行、测试、迁移、Docker 和压测命令；GitHub Actions 执行 golangci-lint、go test、race、vet、go build 和 Docker 镜像构建。
```

暂时不要写：

- “彻底解决并发超卖问题”；
- “数据库乐观锁”作为核心实现，除非改成“条件扣减”或增加 version 字段；
- “分布式事务保证订单创建与库存扣减最终一致”，当前是同库事务，不是跨服务事务；
- “Outbox 确保消息必达”，应改为“提高消息可靠性，支持失败重试和幂等消费”；
- “Redis 故障自动熔断”；
- “防止缓存击穿拖垮数据库”；
- “全套微服务环境”，当前更准确是“单体应用及依赖组件的容器化编排”。
